name: app_gestion_contacts
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  firebase_core: ^2.32.0
  firebase_auth: ^4.6.0
  cloud_firestore: ^4.17.5
  uuid: ^4.4.0
  shared_preferences: ^2.2.3
  cupertino_icons: ^1.0.8

# Firebase SEULEMENT sur mobile
dev_dependencies:
  build_runner: ^2.4.6
  hive_generator: ^2.0.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true